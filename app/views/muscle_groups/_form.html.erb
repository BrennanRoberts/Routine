<%= form_for(@muscle_group) do |f| %>
  <% if @muscle_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@muscle_group.errors.count, "error") %> prohibited this muscle_group from being saved:</h2>

      <ul>
      <% @muscle_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
